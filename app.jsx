const header = (
  <header>
  <h1>My First React Element!</h1>
  <p>I just learned how to create a React node and render it in to the dom</p>
  </header>
);

ReactDOM.render(
  header,
  document.getElementById('root')
);
